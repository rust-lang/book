use std::env;


fn main() {
    //let args: Vec<String> = env::args().collect();
    //println!("{:?}", args);
	
	//println!("{:#?}", env::args());
	
	//let args: Vec<std::ffi::OsString> = env::args_os().collect();
	//let args = env::args_os().collect();
	//let args: _ = env::args_os().collect();
	println!("{:?}", args);
}