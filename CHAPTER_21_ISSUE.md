# مشكلة خطيرة في الفصل 21

## الوصف
الفصل 21 بأكمله يحتوي على ترجمة مختلطة (عربي + إنجليزي في نفس الجملة)

## الملفات المتأثرة
- `src/ch21-00-final-project-a-web-server.md` (23 سطر - جيد نسبياً)
- `src/ch21-01-single-threaded.md` (271 سطر - يحتاج مراجعة)
- `src/ch21-02-multithreaded.md` (447 سطر - يحتاج إعادة ترجمة كاملة)
- `src/ch21-03-graceful-shutdown-and-cleanup.md` (152 سطر - يحتاج إعادة ترجمة كاملة)

**المجموع**: 893 سطر

## أمثلة على المشكلة

### مثال 1 (من ch21-02-multithreaded.md:8):
```
الآن، سيعالج الخادوم server كل طلب request بدوره in turn، مما يعني أنه لن يعالج اتصالاً connection ثانيًا حتى ينتهي معالجة الاتصال connection الأول.
```

**يجب أن تكون**:
```
الآن، سيعالج الخادوم كل طلب بدوره، مما يعني أنه لن يعالج اتصالاً ثانياً حتى ينتهي من معالجة الاتصال الأول.
```

### مثال 2 (من ch21-02-multithreaded.md:42):
```
بدلاً من توليد spawning خيوط threads غير محدودة unlimited، إذن، سيكون لدينا عدد ثابت fixed number من الخيوط threads في انتظار waiting في المجمع pool.
```

**يجب أن تكون**:
```
بدلاً من توليد خيوط غير محدودة، سيكون لدينا عدد ثابت من الخيوط في انتظار في المجمع.
```

### مثال 3 (من ch21-02-multithreaded.md:437):
```
يُترجم compiles هذا this الكود code ويعمل runs لكن but لا doesn't result in لا ينتج عنه سلوك behavior الخيوط threading المطلوب desired
```

## التوصية
**يحتاج الفصل 21 إلى إعادة ترجمة كاملة من المصدر الإنجليزي الأصلي**

الترجمة الحالية تبدو وكأنها:
1. ترجمة آلية لم يتم تنظيفها
2. أو محاولة لترجمة كلمة بكلمة مع الاحتفاظ بالإنجليزية

هذا يجعل النص:
- غير قابل للقراءة
- مربك للقارئ العربي
- يفقد المعنى الأصلي

## الإحصائيات
- عدد الكلمات الإنجليزية في ch21-02 وحده: أكثر من 500 كلمة
- أنماط متكررة: server, thread, pool, request, connection, spawning, task, worker, etc.

## الخلاصة
لا يمكن "تصحيح" هذا الفصل - يحتاج إلى إعادة ترجمة احترافية كاملة.
